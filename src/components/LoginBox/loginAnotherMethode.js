// src/components/LoginBox/LoginAnotherMethod.js

import React from 'react';
import { Button } from '@mui/material';

const LoginAnotherMethod = () => {
  return (
    <div>
      <Button variant="contained" color="primary">
        Login with Google
      </Button>
    </div>
  );
};

export default LoginAnotherMethod;
